// The reaction emoji used to represent a "Gilla" (like) across the feed and channel messages,
// kept in its own module (no server-only imports) so client components can use it too.
export const FEED_LIKE_EMOJI = "👍";
